- error = @account.errors.key?(:first_name) && @account.errors[:first_name].count > 0
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :first_name, :class => 'control-label'
  .controls
    =f.text_field :first_name, :class => 'form-control input-large input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:first_name, :class => 'text-error') : ''
- error = @account.errors.key?(:last_name) && @account.errors[:last_name].count > 0
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :last_name, :class => 'control-label'
  .controls
    =f.text_field :last_name, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:last_name, :class => 'text-error') : ''
- error = @account.errors.key?(:email) && @account.errors[:email].count > 0
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :email, :class => 'control-label'
  .controls
    =f.text_field :email, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:email, :class => 'text-error') : ''
- error = @account.errors.key?(:password) && @account.errors[:password].count > 0
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :password, :class => 'control-label'
  .controls
    =f.password_field :password, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:password, :class => 'text-error') : ''
- error = @account.errors.key?(:password_confirmation) && @account.errors[:password_confirmation].count > 0
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :password_confirmation, :class => 'control-label'
  .controls
    =f.password_field :password_confirmation, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:password_confirmation, :class => 'text-error') : ''
- error = @account.errors.key?(:role) && @account.errors[:role].count > 0
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :role, :class => 'control-label'
  .controls
    =f.text_field :role, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:role, :class => 'text-error') : ''

.form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:accounts, :index), :class => 'btn btn-default'